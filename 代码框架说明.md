# 代码框架说明

> 本文档描述 Eye Break (Daily Break) 的代码架构、各文件职责和模块间关系。面向 Claude Code / Codex 等 AI 编程助手阅读。

---

## 1. 文件分类与职责

### 1.1 应用入口

| 文件 | 职责 |
|---|---|
| `Eye_BreakApp.swift` | `@main` 入口。设置 `NSApplication.ActivationPolicy.accessory`（无 Dock 图标）。创建 `DailyBreakModel` 作为全局 `@StateObject`。定义两个 Scene：`MenuBarExtra`（菜单栏弹出窗口 + 标签）和 `Settings`（设置面板）。 |

### 1.2 数据模型层

| 文件 | 职责 |
|---|---|
| `BreakModels.swift` | 所有纯数据类型定义。包含：枚举（`BreakKind`、`BreakPhase`、`ActiveDays`、`OverlayIntensity`）、配置结构体（`BreakSettings`，Codable + 默认值 + 前向兼容解码）、统计结构体（`BreakStats`，按天 key 区分）、蒙层状态（`RestOverlayState`）、健康建议库（`AdviceLibrary`）、格式化工具（`formatDuration`）。**此文件无运行时逻辑，只有类型和纯函数。** |

### 1.3 核心状态机

| 文件 | 职责 |
|---|---|
| `BreakTimerEngine.swift` | 纯值类型 `struct`，无 SwiftUI 依赖。管理所有阶段转换（`working → preBreak → resting → working`）和计时逻辑。通过构造函数注入 `now: () -> Date` 和 `Calendar` 实现完全测试隔离。对外暴露 `tick()`、`pause()`、`resume()`、`skipBreak()`、`startBreakNow()`、`resetWorkCycle()`、`systemWillSleepOrLock()`、`systemDidWakeOrUnlock()` 等操作。内部用 `workDeadline`/`restDeadline` 计算剩余秒数，用 `isActiveTime()` 判断是否在活跃时段。由于文件已改写，内部状态变量较多，需重点理解。 |

### 1.4 ViewModel 桥接层

| 文件 | 职责 |
|---|---|
| `DailyBreakModel.swift` | `@MainActor ObservableObject`，应用中**唯一的** ViewModel。持有 `BreakTimerEngine` 实例和所有 service 实例。通过 1 秒 `Timer` 驱动 `engine.tick()` → `syncFromEngine()` 循环，将 engine 状态同步到 `@Published` 属性。所有 View 只与 `DailyBreakModel` 交互，不直接接触 engine。提供 `pause()`、`resume()`、`startBreakNow()`、`skipBreak()`、`resetWorkCycle()` 等用户操作方法。`syncFromEngine()` 中协调通知发送、蒙层显示/关闭、toast 消息和统计持久化。 |

### 1.5 持久化层

| 文件 | 职责 |
|---|---|
| `DailyBreakStore.swift` | 将 `BreakSettings` 和 `BreakStats` 通过 `JSONEncoder`/`JSONDecoder` 存入 `UserDefaults`（key: `dailyBreak.settings` / `dailyBreak.stats`）。`loadStats()` 自动检测跨天并返回当日空统计。构造函数可注入自定义 `UserDefaults` 便于测试。 |

### 1.6 服务层

| 文件 | 职责 |
|---|---|
| `AppServices.swift` | 四个服务类，均无 SwiftUI 依赖：<br>• `NotificationService` — 请求通知授权，发送 "30 秒后休息" 本地通知<br>• `LaunchAtLoginService` — 通过 `SMAppService.mainApp` 管理登录自启<br>• `SystemActivityMonitor` — 监听睡眠/锁屏/会话切换等 8 种系统事件，通过 `onAway`/`onReturn` 回调通知<br>• `RestOverlayController` — 每个屏幕创建全屏 `NSWindow`（level: `.screenSaver`），嵌入 SwiftUI `RestOverlayView`，提供 `show`/`update`/`close` 方法 |

### 1.7 View 层

| 文件 | 职责 |
|---|---|
| `MenuBarView.swift` | 菜单栏弹出窗口。顶部状态区（标题 + 副标题 + 下次休息提示），中间健康建议卡片（图标 + 文案 + 进度条），操作区（暂停/恢复、立即休息、重置），底部区（设置、退出）。包含子组件：`ActionGroup`、`MenuActionRow`、`MenuSeparator`、`ProgressBar`、`ToastView`、`Color` 扩展。 |
| `SettingsView.swift` | 设置面板。使用 `.formStyle(.grouped)`，6 个 Section。含保存/撤销操作栏（修改后显示）和修改 toast。时间点用 `DatePicker(.stepperField)`，持续时长用 `Picker` 或 `TextField`。 |
| `RestOverlayView.swift` | 全屏休息蒙层。主屏幕显示呼吸动画 Circle、休息标题、倒计时、进度条、健康建议、跳过按钮。副屏幕显示简化版。包含 `RestOverlayWindowModel`（ObservableObject）、`BreathingCircle`（缩放动画）。 |
| `RestIcon.swift` | 菜单栏标签中休息阶段显示的冥想 SVG 图标。通过 `NSImage` 加载 SVG 并设为 `isTemplate = true` 适配明暗模式。 |

### 1.8 测试

| 文件 | 职责 |
|---|---|
| `BreakTimerEngineTests.swift` | `BreakTimerEngine` 单元测试。使用 `TestClock`（可变 value type）注入日期，实现确定性测试。覆盖：阶段转换、暂停/恢复、跳过、系统离场/返回、活跃时间段边界、休息周期切换、锁屏睡眠规则。 |

---

## 2. 数据流

```text
用户操作 / 系统事件
        │
        ▼
  DailyBreakModel  ──(1s Timer)──►  BreakTimerEngine.tick()
        │                                    │
        │◄────── syncFromEngine() ───────────┘
        │
   ┌────┼────┬──────────┬──────────┐
   ▼    ▼    ▼          ▼          ▼
  View 通知 蒙层       Toast    持久化
 (SwiftUI)  (UNNotification) (NSWindow) (ToastView) (DailyBreakStore → UserDefaults)
```

## 3. 状态机

参见 `BreakTimerEngine.swift` 和 `REQUIREMENTS.md` §3。

`BreakPhase` 枚举定义 7 个状态。`BreakTimerEngine` 持有 `workDeadline`/`restDeadline` 两个内部截止时间，`tick()` 中根据当前 phase 调用对应 countdown 方法。

外部操作（pause/resume/skip/startBreakNow/resetWorkCycle）通过 engine 的 mutating func 修改状态，再由 `syncFromEngine()` 将变化同步到 UI 和服务层。

## 4. 关键设计决策

1. **时间注入**：`BreakTimerEngine` 不直接调用 `Date()`，而是通过 `now: () -> Date` 闭包。测试中使用 `TestClock`，生产代码中使用 `Date.init`。
2. **单一 ViewModel**：整个应用只有一个 `DailyBreakModel`（`@MainActor`），避免多个 ObservableObject 之间的同步问题。
3. **持久化自动触发**：`settings` 的 `didSet` 自动保存到 `UserDefaults` 并更新 engine，无需手动调用 save。
4. **解码前向兼容**：`BreakSettings` 的 `init(from decoder:)` 对所有字段使用 `decodeIfPresent`，缺失键回退到 `defaults`。
5. **菜单栏无 Dock 图标**：`NSApplication.shared.setActivationPolicy(.accessory)`，符合 macOS 菜单栏应用规范。
