# Design.md

# Daily Break / Eye Break 设计规范

## 1. 设计定位

Daily Break 是一款运行在 macOS 菜单栏中的休息提醒应用。

它不是一个强提醒工具，也不是一个冰冷的效率计时器，而是一个温柔的健康陪伴小助手。

设计目标是：

* 温和提醒用户休息
* 降低长时间工作带来的眼疲劳和身体疲劳
* 不打断用户的工作状态
* 在视觉上给用户一种轻松、清爽、被照顾的感觉

产品气质关键词：

* 温馨
* 柔和
* 清爽
* 安静
* 轻盈
* 有呼吸感
* macOS 原生感
* 健康陪伴感

不要做成：

* 警告感很强的倒计时工具
* 黑白灰很硬的系统控制面板
* 过度商业化的健康 App
* 花哨、卡通、幼稚的界面
* 视觉重量过重的大弹窗

核心感觉应当是：

> 我不是在催你，我是在提醒你照顾自己。

---

## 2. 总体视觉方向

整体使用 macOS 风格的半透明磨砂玻璃面板，搭配柔和阴影、圆角卡片、浅蓝绿与暖色中性色，营造轻松、舒适、安静的视觉体验。

界面需要有设计感，但不能过度装饰。

建议方向：

* 背景：轻微磨砂、半透明、浅色
* 面板：圆角、柔和阴影、macOS popover 质感
* 色彩：浅蓝、青绿、薄荷绿、暖白、浅灰
* 图标：SF Symbols 风格，线性、柔和、低压迫感
* 字体：系统字体，字重克制
* 文案：温柔、自然、陪伴式表达
* 动效：轻微、缓慢、无攻击性

---

## 3. 菜单栏面板设计

菜单栏点击后的弹出面板是产品最核心的日常入口。

面板应当像一个温柔的休息卡片，而不是普通工具菜单。

### 3.1 面板尺寸

当前菜单栏面板应采用接近 macOS 原生菜单的紧凑尺寸，而不是大卡片弹窗。

建议尺寸：

* 宽度：300px - 330px，默认约 320px
* 高度：根据内容自适应，避免超过普通菜单的视觉体量
* 圆角：16px - 20px
* 内边距：16px 左右
* 顶部留白：16px - 20px
* 底部留白：14px - 18px

面板不能过宽，也不能太像设置窗口。

它应该是一个轻量菜单栏 popover，视觉体量接近系统输入法菜单，而不是独立设置窗口。

---

## 4. 菜单栏面板结构

当前推荐结构：

```text
还有 04:04 可以休息一下
短休息 3 次 · 长休息 0 次

[健康建议卡片]
看看远处，放松一下眼睛
[进度条]

[操作区]
暂停
现在休息一下
重新开始本轮

[底部操作区]
设置
退出 Daily Break
```

紧凑版中每个模块都需要收窄：

* 顶部标题一行优先，过长时压缩字号，不换成大段文字。
* 健康建议卡片高度控制在 72px - 84px。
* 主操作行高度控制在 42px - 48px。
* 图标圆底控制在 30px - 34px。
* 分组间距控制在 10px - 12px。

---

## 5. 顶部状态区

### 5.1 标题

推荐文案：

```text
还有 04:04 可以休息一下
```

不要使用过于命令式或警告式的表达，例如：

```text
距离休息还有 04:04
必须休息
即将强制休息
```

标题应该传达“可以休息一下”的轻松感。

### 5.2 标题样式

建议：

* 字号：18px - 22px
* 字重：Semibold
* 颜色：深蓝灰 / 深灰，不使用纯黑
* 居中展示
* 紧凑菜单下不宜过大

推荐视觉效果：

```text
还有 04:04 可以休息一下
```

它应该醒目，但不压迫。

### 5.3 副标题

推荐文案：

```text
短休息 3 次 · 长休息 0 次
```

样式：

* 字号：12px - 14px
* 字重：Regular / Medium
* 颜色：Secondary Text
* 居中展示
* 与标题保持 4px - 8px 间距

---

## 6. 健康建议卡片

健康建议卡片是温馨感的关键来源。

它让页面从“计时器”变成“健康陪伴工具”。

### 6.1 卡片内容

推荐文案：

```text
看看远处，放松一下眼睛
```

其他可随机出现的文案：

```text
慢慢眨眼，让眼睛休息一下
喝口水，活动一下肩颈
深呼吸一下，放松身体
站起来走一走
离开屏幕一小会儿
```

### 6.2 卡片样式

建议：

* 背景：半透明暖白
* 圆角：16px - 20px
* 内边距：20px - 24px
* 阴影：极轻，不要厚重
* 可带一个柔和图标，例如叶子、眼睛、水杯

### 6.3 图标

健康建议卡片建议使用柔和图标。

推荐：

* leaf
* eye
* cup.and.saucer
* drop
* sparkles
* sun.min

图标颜色建议：

* 薄荷绿
* 浅青绿
* 柔和蓝
* 灰蓝色

图标可以放在浅色圆形底中，提升温馨感。

---

## 7. 进度条

进度条用于表达当前工作周期进度。

它不应有强烈的倒计时压迫感。

### 7.1 样式

建议：

* 高度：4px - 6px
* 圆角：完整圆角
* 背景：浅灰蓝
* 前景：柔和青绿 / 浅蓝绿
* 不使用高饱和红色、橙色

### 7.2 位置

进度条放在健康建议文案下方。

结构：

```text
看看远处，放松一下眼睛
[柔和进度条]
```

---

## 8. 操作区

操作区使用菜单行样式，不使用厚重按钮。

推荐操作：

```text
暂停
现在休息一下
重新开始本轮
```

### 8.1 文案原则

文案应自然、温和、清楚。

推荐：

| 功能     | 推荐文案           |
| ------ | -------------- |
| 暂停提醒   | 暂停             |
| 立即进入休息 | 现在休息一下         |
| 重置当前计时 | 重新开始本轮         |
| 打开设置   | 设置             |
| 退出应用   | 退出 Daily Break |

避免使用过于生硬的文案：

```text
立即执行
强制休息
重置任务
终止程序
```

### 8.2 行样式

每一行建议：

* 高度：40px - 46px
* 圆角：12px - 16px
* 背景：默认白色/半透明
* Hover：浅蓝灰或浅青色背景
* 左侧图标
* 中间文字
* 右侧可选 chevron

行与行之间可以有极细分割线，或者以卡片分组呈现。

### 8.3 图标建议

| 操作             | 图标方向                                       |
| -------------- | ------------------------------------------ |
| 暂停             | pause.fill / pause.circle                  |
| 现在休息一下         | cup.and.saucer / leaf / eye                |
| 重新开始本轮         | arrow.clockwise                            |
| 设置             | gearshape                                  |
| 退出 Daily Break | power / rectangle.portrait.and.arrow.right |

图标应该轻，不要过粗。

建议为每个图标增加柔和圆形底：

* 暂停：浅蓝
* 休息：薄荷绿
* 重置：浅紫
* 设置：浅灰
* 退出：浅灰蓝

---

## 9. 底部操作区

底部包含低频操作：

```text
设置
退出 Daily Break
```

它们不应该抢占主操作区视觉。

建议：

* 与主操作区之间留 16px - 20px 间距
* 可使用一条浅分割线
* 文字颜色略弱于主操作
* “退出 Daily Break”不需要做红色

---

## 10. 色彩规范

整体色彩应柔和、低饱和。

### 10.1 主色方向

推荐：

* Soft Blue
* Mint Green
* Aqua
* Warm White
* Blue Gray
* Light Lavender

### 10.2 避免颜色

避免大面积使用：

* 纯黑
* 高饱和红
* 高饱和橙
* 荧光绿
* 强紫色
* 大面积深色背景

### 10.3 参考色感

可参考以下方向：

```text
背景：暖白 / 浅蓝白
主强调：柔和蓝绿
辅助：浅灰蓝
健康提示：薄荷绿
警示：尽量不用红色
```

---

## 11. 字体与层级

使用 macOS 系统字体。

### 11.1 字体层级

标题：

* 18px - 22px
* Semibold

副标题：

* 12px - 14px
* Regular / Medium
* Secondary color

健康建议：

* 13px - 15px
* Medium

菜单项：

* 13px - 15px
* Regular / Medium

辅助说明：

* 11px - 13px
* Secondary color

### 11.2 字重原则

不要全部加粗。

只有当前最重要的信息，例如倒计时标题，可以使用较强字重。

其他内容保持轻盈。

---

## 12. 圆角与阴影

### 12.1 圆角

推荐：

* 主面板：18px - 24px
* 健康建议卡片：16px - 20px
* 菜单行：12px - 16px
* 图标圆底：圆形

圆角要统一，不要一会儿大圆角一会儿小圆角。

### 12.2 阴影

阴影要轻、柔、扩散。

建议：

* 不使用厚重黑影
* 不使用强边框
* 使用类似 macOS popover 的自然浮层阴影

面板要像轻轻浮在桌面上，而不是压在屏幕上。

---

## 13. 磨砂玻璃与透明感

菜单栏面板建议使用 macOS 原生或接近原生的磨砂效果。

视觉方向：

* 半透明
* 背景轻微模糊
* 保持文字可读性
* 不要让背景干扰内容
* 不要过度透明

健康类产品需要清爽，但不能因为透明导致信息不清楚。

---

## 14. 动效原则

动效要柔和、克制。

推荐：

* 面板打开：轻微 fade + scale
* Hover：背景轻微变化
* 进度条：平滑变化
* 休息蒙层：缓慢淡入
* 休息结束：柔和淡出

避免：

* 快速弹跳
* 强烈震动
* 警告闪烁
* 高频动效
* 过度拟物动画

动效目标是“放松”，不是“吸引注意力”。

---

## 15. 交互状态

### 15.1 工作中

显示：

```text
还有 04:04 可以休息一下
短休息 3 次 · 长休息 0 次
看看远处，放松一下眼睛

暂停
现在休息一下
重新开始本轮
设置
退出 Daily Break
```

### 15.2 已暂停

标题可变为：

```text
已暂停
```

副标题：

```text
点击继续后恢复倒计时
```

操作项：

```text
继续计时
现在休息一下
重新开始本轮
设置
退出 Daily Break
```

### 15.3 休息中

标题可变为：

```text
正在休息 00:18
```

健康建议：

```text
看看远处，放松一下眼睛
```

操作项：

```text
结束休息
延长 1 分钟
设置
退出 Daily Break
```

### 15.4 非执行时间

标题可变为：

```text
今天先休息吧
```

副标题：

```text
明天 09:00 自动开始
```

操作项：

```text
立即开始
设置
退出 Daily Break
```

---

## 16. 设置页设计

设置页是低频但重要的配置入口，应当比菜单栏弹层更宽裕，但仍保持 Daily Break 的温柔、清爽、macOS 原生感。

它不应像默认系统表单那样冰冷，也不应做成复杂后台控制台。推荐使用浅色磨砂窗口、分区卡片、行式控件和柔和图标，形成清楚但轻盈的设置体验。

### 16.1 窗口与整体布局

推荐结构：

```text
Daily Break 设置

[通用设置]
  开机自动启动        Toggle
  菜单栏显示倒计时    Toggle

[时间安排]
  工作时长            [20] 分钟
  重复                每天 / 自定义
  自定义星期          周一 ... 周日
  工作可用            Toggle
    开始时间          09:00
    结束时间          17:00
    跳过午休          Toggle
      午休开始        12:00
      午休结束        13:00

[休息周期]
  短休息时长          [20] 秒
  长休息设置          Toggle
    间隔              [3] 次短休息进行 1 次长休息
    长休息时长        [3] 分钟

[休息界面]
  蒙层强度            弱 / 中 / 强
  动画效果            Toggle

[仅有编辑时底部悬浮：撤销 / 保存]
```

建议尺寸：

* 窗口宽度：660px - 720px，默认约 690px
* 内容最大宽度：560px - 620px
* 顶部标题居中，字号 19px - 21px，Semibold
* 外层圆角：22px - 28px
* 分区间距：14px - 18px
* 页面底部默认不占用保存区域；仅在存在未保存编辑时显示底部固定悬浮操作条

### 16.2 分区卡片

每个设置模块使用独立卡片：

* 背景：半透明暖白 / `.ultraThinMaterial`
* 圆角：18px - 22px
* 内边距：14px - 18px
* 边框：1px 白色半透明
* 阴影：极轻

分区标题左侧使用柔和圆形图标底：

| 分区 | 图标方向 | 色彩 |
| --- | --- | --- |
| 通用设置 | leaf / power / clock | 薄荷绿 |
| 时间安排 | calendar | 柔和蓝 |
| 休息周期 | cup.and.saucer | 薄荷绿 |
| 休息界面 | display | 浅紫蓝 |

标题字号建议 16px - 18px，字重 Semibold，颜色使用深蓝灰。

### 16.3 设置行

设置项使用行式布局，不使用厚重按钮：

* 行高：42px - 50px
* 行背景：白色半透明
* 行圆角：14px - 16px
* 行内左右 padding：14px - 18px
* 分割线：非常浅的灰蓝色
* 左侧为设置名称，右侧为控件
* 所有行的右侧控件必须进入统一 `SettingsControlSlot`，由 slot 管理右侧控制列和右对齐，不允许每个控件自行散落 `.frame(width:..., alignment:.trailing)`
* 右侧具体内容的最右边缘必须对齐；数字输入、时间选择器、分段控件和 Toggle 都落在同一个控制列内，并为主要可见控件设置标准宽度
* `休息周期 > 间隔` 使用数字输入框，单位为“次”，不使用下拉选择
* `休息周期 > 长休息时长` 使用数字输入框，单位为“分钟”，不使用下拉选择
* 带说明文字的设置项不要作为单独设置行；说明文字应作为 `SettingsRow` 的 subtitle 放在主标题下方，不参与右侧控件列对齐

行内控件建议：

* Toggle：使用青绿色强调色
* 数字输入：右侧小型 stepper/text field，旁边可有单位胶囊
* 时间选择：右侧 `TimeInlineControl` 行内编辑，24 小时制 HH:mm，不弹出额外窗口
* 分段选择：弱 / 中 / 强、每天 / 自定义等使用自绘 `SettingsSegmentedControl`，不使用系统 `Picker(.segmented)`
* 星期选择：使用横排 checkbox，默认工作日勾选

重复选项只保留：

```text
每天 / 自定义
```

不在设置页展示“周一至周五”。如果底层历史配置里存在工作日模式，设置页打开时按“每天”展示，用户保存后写回当前可见选项。

### 16.4 保存按钮

保存按钮位于右下角，不铺满整行。

建议：

* 宽度：150px - 180px
* 高度：44px - 50px
* 圆角：12px - 16px
* 背景：柔和青绿
* 文字：白色，Semibold
* 只有存在未保存修改时更醒目；没有修改时可以降低透明度或禁用

### 16.5 设置页约束

* 设置页必须保留草稿编辑语义：编辑控件只修改本地草稿，点击保存后才写入运行中的设置。
* 撤销或关闭窗口前的未保存内容不能影响当前倒计时。
* UI 条件展示必须跟随草稿，例如切换“自定义”后立即展示星期选择，但不立刻影响状态机。
* 不要让设置页变成营销页或说明页；第一屏直接是可操作设置。

---

## 17. 休息蒙层设计方向

休息蒙层应与菜单栏面板保持同一套视觉语言。

关键词：

* 柔和
* 安静
* 浅色
* 低压迫
* 健康陪伴
* 轻微遮挡，而非强制锁屏

休息蒙层内容建议：

```text
休息一下
看看远处，放松眼睛
00:20
[进度条]
```

不要使用：

```text
禁止操作
必须休息
强制休息中
```

除非用户主动开启严格模式。

---

## 18. 文案语气规范

Daily Break 的文案要像一个温柔的提醒者。

### 18.1 推荐语气

* 可以休息一下
* 看看远处
* 放松一下眼睛
* 喝口水
* 站起来走一走
* 现在休息一下
* 重新开始本轮

### 18.2 不推荐语气

* 你必须休息
* 强制休息
* 立即停止工作
* 休息失败
* 任务中断
* 禁止继续

产品不应制造压力。

---

## 19. 当前菜单栏推荐稿

当前推荐菜单栏面板内容如下：

```text
还有 04:04 可以休息一下
短休息 3 次 · 长休息 0 次

看看远处，放松一下眼睛
[进度条]

暂停
现在休息一下
重新开始本轮

设置
退出 Daily Break
```

这是当前设计方向的基准版本。

后续所有菜单栏设计调整，都应在此基础上继续优化，不要偏离“温柔、清爽、macOS 原生感、健康陪伴感”的方向。

---

## 20. 设计检查清单

每次调整 UI 前，检查以下问题：

1. 是否足够温柔？
2. 是否像 Mac 原生菜单栏应用？
3. 是否有清楚的信息层级？
4. 是否避免了过强的警告感？
5. 是否避免了按钮过重？
6. 是否保留了足够留白？
7. 是否使用了柔和色彩？
8. 是否让用户感觉被提醒，而不是被控制？
9. 是否能一眼看懂当前状态？
10. 是否符合健康陪伴工具的产品气质？

---

## 21. 总结

Daily Break 的设计核心不是“倒计时”，而是“帮助用户更轻松地照顾自己”。

界面要有设计感，但不能喧宾夺主。

最重要的设计方向是：

> 温柔提醒，轻轻陪伴，让用户愿意休息，而不是被迫休息。
