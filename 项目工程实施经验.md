# 项目工程实施经验

## 概要目录

更新时间：2026-06-24 13:48:30 CST

| Section | Keywords | Read When |
| --- | --- | --- |
| [问题修复经验记录机制](#问题修复经验记录机制) | 工程经验, bug修复, skill | 记录或更新工程经验前 |
| [主子 Agent 编码协作流程](#主子-agent-编码协作流程) | 主Agent, 子Agent, 架构审核, 技术选型, 写代码, 测试验收 | 需要修改代码、修 bug、实现模块功能前 |
| [菜单栏弹层设计要先同步设计规范再实现](#菜单栏弹层设计要先同步设计规范再实现) | 菜单栏, SwiftUI, MenuBarExtra, Design.md | 修改菜单栏弹层 UI 前 |
| [休息统计应按触发记录而不是只按完整完成记录](#休息统计应按触发记录而不是只按完整完成记录) | 计时状态机, 休息统计, restHistory, 长休息 | 修改休息统计、跳过、长休息间隔前 |
| [锁屏睡眠规则要区分工作倒计时和休息倒计时](#锁屏睡眠规则要区分工作倒计时和休息倒计时) | 锁屏, 睡眠, systemAway, 工作倒计时, 休息倒计时 | 修改系统离开、恢复、倒计时冻结逻辑前 |
| [设置页草稿与日程规则要分离保存语义和运行语义](#设置页草稿与日程规则要分离保存语义和运行语义) | 设置页, UserDefaults, BreakSettings, activeDays, 午休暂停, 右侧对齐, SettingsControlSlot, 时间胶囊, 24小时制, 数字居中, 保存浮层 | 修改设置页保存、日程生效规则、控件布局或输入形态前 |
| [MenuBarExtra label 只允许简单 SwiftUI 视图](#menubarextra-label-只允许简单-swiftui-视图) | 菜单栏, MenuBarExtra, NSImage, SVG, label, SF Symbol, 渲染异常 | 修改菜单栏图标或 label 前 |
| [Xcode 测试缓存可能造成旧符号加载失败](#xcode-测试缓存可能造成旧符号加载失败) | Xcode, clean test, DerivedData, Symbol not found | 遇到测试 bundle、旧符号、缓存类失败时 |

## 问题修复经验记录机制

更新时间：2026-06-23 15:18:59 CST

关键词：工程经验, bug修复, 经验沉淀, skill, 项目规范

问题：
项目迭代中多次出现通过对话修复 bug 或调整问题，但工程经验没有沉淀，后续相似问题容易重复排查。

工程经验：
- 每次修复 bug、修改既有问题或用户要求总结过往工程问题经验时，都要更新本文档。
- 相同或相似问题不要新增重复章节，应修改已有章节并更新“更新时间”。
- 经验记录应聚焦根因、预防规则和验证方式，不记录冗长聊天过程。
- 写代码前先用相关模块关键词搜索本文档，读完匹配 section 再改实现。
- 每个 section 必须维护“关键词”，便于后续按模块、症状和技术点检索。
- 文档顶部必须维护 `概要目录`，让 agent 先快速判断要读哪些详细 section，实现动态上下文拓展。

验证：
- 新增项目 skill `skills/engineering-experience-log/SKILL.md`，用于约束后续记录流程。
- 更新 skill，加入“写代码前先查相关模块经验”的前置步骤。
- 更新 skill，要求每次新增或修改经验 section 时同步维护 `概要目录`。

## 主子 Agent 编码协作流程

更新时间：2026-06-24 10:34:04 CST

关键词：主Agent, 子Agent, 写代码, 测试验收, 代码框架, 项目框架, 架构审核, 技术选型, 主逻辑, 实现代价, 动态上下文, module-subagent-coding

问题：
复杂代码修改中，如果主 agent 同时负责框架判断、具体编码和验收，容易在细节实现中丢失项目结构判断，或在测试失败后直接局部乱改。

工程经验：
- 非平凡代码修改应使用主子 agent 分工：主 agent 负责理解项目框架、代码框架、状态模型、测试策略和验收；子 agent 负责在明确边界内写实现。
- 主 agent 不是只派活，还必须先审核架构、代码框架、主逻辑、技术选型、模块边界和实现代价是否合理。
- 主 agent 和子 agent 在动手前都必须先读 `项目工程实施经验.md` 的 `概要目录`，再按关键词读取相关详细章节。
- 主 agent 写或更新测试，并以测试/构建结果验收；如果不通过，把失败输出和期望行为交回子 agent 修正。
- 子 agent 的任务描述必须包含相关文件、经验章节关键词、约束和验收标准，避免扩大修改范围。
- 子 agent 只做主 agent 批准过的具体实现；不得自行改架构、模块边界、技术选型或主逻辑。
- 小型一行修改或没有子 agent 工具时，可以不派发子 agent，但仍要执行经验查阅、框架思考、测试和验收流程。

验证：
- 新增项目 skill `skills/module-subagent-coding/SKILL.md`，定义主/子 agent 编码职责和验证闭环。
- 更新 `.codex/skills/module-subagent-coding/SKILL.md` 和 `.claude/skills/module-subagent-coding/SKILL.md`，加入主 agent 架构审核 gate。

## 菜单栏弹层设计要先同步设计规范再实现

更新时间：2026-06-24 11:23:33 CST

关键词：菜单栏, MenuBarExtra, SwiftUI, Design.md, UI尺寸, 紧凑菜单, macOS原生感

问题：
菜单栏面板一开始按大卡片设计实现，实际运行后视觉体量过大，不符合用户希望接近系统菜单的尺寸。

工程经验：
- 菜单栏 UI 调整前先读 `Design.md` 和用户截图，先更新设计规范，再改 SwiftUI 实现。
- `MenuBarExtra` 弹层应优先控制整体宽度、行高、图标尺寸和分组间距；不要只改局部样式。
- 大标题、健康卡片和操作行需要按菜单场景压缩，不应套用设置页或独立窗口的视觉尺度。
- 视觉类修改也要跑 `xcodebuild ... build`，避免 SwiftUI 泛型或布局代码编译失败。

验证：
- 将菜单宽度从 420px 收紧到约 320px，并通过 `xcodebuild -project "Eye Break.xcodeproj" -scheme "Eye Break" -configuration Debug CODE_SIGNING_ALLOWED=NO build`。

## 休息统计应按触发记录而不是只按完整完成记录

更新时间：2026-06-23 15:03:37 CST

关键词：计时状态机, 休息统计, BreakTimerEngine, BreakStats, restHistory, 跳过休息, 长休息

问题：
休息计数和触发历史如果混用，会在两个方向出错：只在 `completeRest()` 里记录会导致跳过不计入长休息间隔；在触发时直接增加 `shortBreaks/longBreaks` 又会让菜单“今日休息次数”把跳过算成完成。

工程经验：
- 统计口径要先定义清楚：菜单和持久化的 `BreakStats.shortBreaks/longBreaks` 表示完整完成；`restHistory` 表示触发历史。
- 长休息触发不能只依赖完成次数，必须使用 `restHistory` 记录完成和跳过都会产生的触发类型。
- 跳过短休息仍计入长休息间隔；跳过长休息后间隔应重置，下一次为短休息。
- `startRest` 和 preBreak/working/postponed 阶段的 `skipBreak` 只记录触发；`completeRest` 才增加完成计数。
- 修改状态机时必须加单元测试覆盖触发、完成、跳过、长休息重置等路径。

验证：
- 新增 `restHistory`，并增加跳过短休息计入长休息间隔、跳过长休息后下一次为短休息等测试。
- 补充验证触发休息时完成计数仍为 0、完整休息结束才增加菜单计数。
- `xcodebuild -project "Eye Break.xcodeproj" -scheme "Eye Break" -configuration Debug CODE_SIGNING_ALLOWED=NO test` 通过，18 个测试 0 失败。

## 锁屏睡眠规则要区分工作倒计时和休息倒计时

更新时间：2026-06-23 15:03:37 CST

关键词：锁屏, 睡眠, 关屏, systemAway, 工作倒计时, 休息倒计时, BreakTimerEngine

问题：
系统离开时原先把工作和休息统一冻结，导致休息中锁屏后返回仍显示休息，而用户期望休息期间离开也算休息时间。

工程经验：
- 系统离开不是单一规则：工作倒计时应冻结或在离开时间超过剩余工作时间时重置；休息倒计时应按真实离开时间继续消耗。
- 记录 `awayStartedAt` 和离开前 phase，恢复时按不同 phase 分支处理，不要只保存 remainingSeconds 后直接恢复。
- 工作中离开时间 `>= remainingSeconds` 时，恢复后直接开始新的工作周期，不补弹休息蒙层。
- 休息中离开超过剩余休息时间时，恢复后直接进入下一轮工作倒计时；未超过则恢复剩余休息时间。

验证：
- 增加 `testSystemAwayLongerThanRemainingWorkTimeResetsWorkCycle`、`testSystemAwayDuringRestCountsDownAndStartsNextWorkCycleWhenRestElapsed`、`testSystemAwayDuringRestResumesRemainingRestWhenNotElapsed`。
- `xcodebuild ... test` 通过。

## 设置页草稿与日程规则要分离保存语义和运行语义

更新时间：2026-06-24 17:08:00 CST

关键词：设置页, UserDefaults, BreakSettings, activeDays, everyday, 午休暂停, 保存, 撤销, 右侧对齐, 固定控制列, SettingsControlSlot, SettingsSegmentedControl, TimeInlineControl, subtitle, 24小时制, 数字居中, 数字输入, 保存浮层

问题：
设置页如果控件直接绑定 `model.settings`，用户编辑时会立即触发 UserDefaults 持久化和 engine 设置同步，导致”保存/撤销”只是视觉状态；日程判断如果把 `.everyday` 理解成全天运行，则会绕过开始/结束窗口和午休暂停；设置页视觉调整如果只改局部控件宽度，或只在 `content.frame(width:..., alignment:.trailing)` 上做文章，会造成右侧 Toggle、时间选择器、分段控件和数字输入的可见边缘不齐。系统 segmented picker、DatePicker(.stepperField) 的内部 padding、AM/PM、弹窗或上下箭头会破坏当前柔和设置页的统一感；数字输入框默认右对齐导致工具感强。自定义星期塞在右侧控件列内导致和主控件抢对齐空间。

工程经验：
- 设置页需要本地 `draftSettings`，控件只读写草稿；Save 才一次性写入 `model.settings`，Undo 只恢复草稿。
- `activeDays` 只负责日期是否活跃；每日开始/结束窗口和午休暂停必须对 `.everyday`、`.weekdays`、`.custom` 都生效。
- UI 条件渲染也要跟随草稿而不是 `model.settings`，否则未保存的选择不会正确展开或收起子项。
- 设置页所有行的右侧控件必须落在统一 `SettingsControlSlot` 中，由 slot 管理固定宽度控制列和右对齐；不要让每个控件单独决定行尾位置。
- `SettingsControlSlot` 之外还要给 Toggle、Picker、数字输入等主要可见控件设置标准宽度；只对外层 content 做 frame 不能保证系统控件的视觉右边缘一致。
- **分段控件**：不要使用系统 `Picker(.segmented)` 做“每天 / 自定义”或“弱 / 中 / 强”；使用自绘 `SettingsSegmentedControl`，让控件可见背景等于 frame 边界，才能和其它右侧控件真正对齐。
- **时间控件**：macOS 设置页不应使用 `DatePicker(.stepperField)`，也不应再弹出额外 popover。改用 `TimeInlineControl`，24 小时制 HH:mm，直接在当前行编辑，右侧按钮按固定步长调整。
- **说明文案**：说明文案不要作为独立 `SettingsHelpText` 行插入分割线之间；应作为 `SettingsRow` 的 `subtitle` 放在主标题下方，不参与右侧控件列对齐。
- **数字输入**：TextField 数字框统一居中（`.multilineTextAlignment(.center)`），不要右对齐。居中更自然，降低”工具感”。
- **展开子配置不参与右侧控件列**：自定义星期（”重复”的子项）应作为下方完整区域展示，不进入 `SettingsControlSlot`；长休息说明文案应作为父开关行 subtitle，而不是夹在子项之间。
- 数字型业务配置优先使用过滤整数的 `TextField` 草稿绑定，不要为了视觉方便退回固定 Picker。
- 保存和撤销只在 `hasChanges` 为真时以底部固定浮层显示；不要在滚动内容底部常驻占位或渲染禁用保存按钮。

验证：
- `BreakTimerEngineTests.testEverydayScheduleStillRespectsActiveWindowAndLunchPause` 覆盖 `.everyday` 仍遵守时间窗口和午休暂停。
- `xcodebuild -project “Eye Break.xcodeproj” -scheme “Eye Break” -configuration Debug CODE_SIGNING_ALLOWED=NO test` 通过，18 个测试 0 失败。
- `SettingsSegmentedControl` 替换系统 segmented picker、`TimeInlineControl` 替换 DatePicker/popover、说明文案改为 `SettingsRow subtitle` 后 `xcodebuild ... build` 与 `xcodebuild ... test` 通过。
- 自定义星期改为完整展开区域、数字居中后 `xcodebuild ... build` 与 `xcodebuild ... test` 通过。

## MenuBarExtra label 只允许简单 SwiftUI 视图

更新时间：2026-06-24 15:00:00 CST

关键词：菜单栏, MenuBarExtra, NSImage, SVG, label, SF Symbol, 渲染异常, HStack, Image

问题：
菜单栏休息倒计时在 rest 阶段突然不显示。代码逻辑正确（`if case .resting = model.phase` + `Text(model.menuBarTitle)`），17 个测试全过，但菜单栏实际空白。根因是 `MenuBarExtra` 的 label 区域由 macOS 菜单栏渲染，不是完整 SwiftUI 环境：`Image(nsImage:)` 加载 SVG 在这个受限环境里不可靠，会导致整个 `HStack`（含 Text）不渲染。

工程经验：
- `MenuBarExtra` label 中**只能使用 `Text` 和 `Image(systemName:)`**，不能使用 `Image(nsImage:)`、`NSImage(contentsOf:)`、自定义 Shape 或 SVG 渲染。
- `HStack` 在 label 中理论可行但配合不可靠控件会整体失效；优先保持 label 为单一 `Text` 或 `Image(systemName:)`。
- 菜单栏图标使用 SF Symbol 而非自定义 SVG/PNG；SF Symbol 在菜单栏环境中完全可靠。
- `.pickerStyle(.wheel)` 是 iOS 专用 API，macOS 编译直接报错 `'wheel' is unavailable in macOS`；macOS 上 Picker 默认样式即可。

验证：
- 将 `RestIcon()`（NSImage SVG）改为 `Image(systemName: "figure.mind.and.body")` 后，菜单栏休息倒计时立即恢复正常。
- `xcodebuild ... build` 通过，`xcodebuild ... test` 18/18 通过。

## Xcode 测试缓存可能造成旧符号加载失败

更新时间：2026-06-23 15:03:37 CST

关键词：Xcode, xcodebuild, clean test, DerivedData, 测试缓存, Symbol not found, 初始化器签名

问题：
修改 `BreakSettings` 初始化结构后，普通 test 曾因旧测试 bundle 引用旧符号失败，表现为 `Symbol not found`，但 clean 后通过。

工程经验：
- 当 Swift model/init 签名变化后，若测试报旧符号或 bundle 加载异常，先怀疑 DerivedData/test bundle 缓存。
- 优先运行 `xcodebuild ... clean test` 验证是否缓存问题，再改业务代码。
- 不要把缓存导致的加载失败误判为状态机或模型逻辑错误。

验证：
- 使用 `xcodebuild -project "Eye Break.xcodeproj" -scheme "Eye Break" -configuration Debug CODE_SIGNING_ALLOWED=NO clean test` 解除旧符号问题。
